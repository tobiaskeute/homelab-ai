services:
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    env_file: "openwebui.env"
    volumes:
      - open-webui:/app/backend/data
volumes:
  open-webui: